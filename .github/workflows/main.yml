on:
  push:
    branches:
      - main

name: Deploy Laravel Backend via FTP

# Prevent overlapping deploys; a new push cancels a slow in-progress upload.
concurrency:
  group: sarvcast-backend-deploy
  cancel-in-progress: true

env:
  FTP_SERVER: "ftp.sarvcast.ir"
  FTP_USERNAME: "my@sarvcast.ir"
  FTP_PASSWORD: "Prof48017421@#"
  SITE_URL: "https://my.sarvcast.ir"
  DEPLOY_TOKEN: "sarvcast-ftp-deploy-x7k9m2"
  TELEGRAM_BOT_TOKEN: "7488407974:AAFl4Ek9IanbvlkKlRoikQAqdkDtFYbD0Gc"
  TELEGRAM_CHAT_ID: "-1002796302613_97"

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.2"
          extensions: mbstring, xml, ctype, json, bcmath, curl, gd, zip, intl, fileinfo, pdo, pdo_mysql
          coverage: none
          tools: composer:v2

      - name: Validate Composer lock file
        run: composer validate --no-check-publish

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install production dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      - name: Package vendor directory
        run: |
          echo "Vendor size before packaging:"
          du -sh vendor
          (cd vendor && zip -r -9 -q ../vendor.zip .)
          ls -lh vendor.zip

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ env.FTP_SERVER }}
          username: ${{ env.FTP_USERNAME }}
          password: ${{ env.FTP_PASSWORD }}
          server-dir: /
          timeout: 600000
          log-level: minimal
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
            vendor/**
            .env
            .env.*
            .github/**
            tests/**
            storage/logs/**
            storage/framework/sessions/**
            storage/framework/views/**
            storage/framework/cache/data/**

      - name: Run migrations and finalize server deploy
        run: |
          echo "::group::Migrations, dependencies, and cache (server)"
          echo "Calling deploy helper: extract vendor.zip, migrate, clear-compiled, config:cache, route:cache, view:cache, optimize, storage:link"

          RESPONSE=$(curl -s -w "\n%{http_code}" \
            "${{ env.SITE_URL }}/_deploy_helper.php?token=${{ env.DEPLOY_TOKEN }}" \
            --max-time 600 --connect-timeout 15)

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')

          echo "HTTP status: $HTTP_CODE"
          echo "Raw response: $BODY"

          if [[ "$HTTP_CODE" != "200" ]]; then
            echo "::error::Deploy helper returned HTTP $HTTP_CODE"
            echo "::endgroup::"
            exit 1
          fi

          STATUS=$(echo "$BODY" | jq -r '.status // "unknown"')
          echo "Helper status: $STATUS"
          echo "Command results:"
          echo "$BODY" | jq -r '.results[]?' 2>/dev/null | while read -r line; do
            echo "  - $line"
          done

          if [[ "$STATUS" != "success" ]]; then
            echo "::error::Server post-deploy failed"
            echo "::endgroup::"
            exit 1
          fi

          PENDING=$(echo "$BODY" | jq -r '.results[]?' 2>/dev/null | grep -c "Pending" || true)
          if [[ "$PENDING" -gt 0 ]]; then
            echo "⚠️ $PENDING pending migration(s) reported before migrate step"
          fi

          echo "✅ Migrations applied, dependencies installed, and caches rebuilt on server"
          echo "::endgroup::"

      - name: Notify Telegram (success)
        if: success()
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B | head -c 200)
          COMMIT_HASH=$(git rev-parse --short HEAD)
          AUTHOR=$(git log -1 --pretty=%an)

          TEXT="✅ *SarvCast Backend Deployed*

          📦 Commit: \`${COMMIT_HASH}\`
          👤 Author: ${AUTHOR}
          📝 ${COMMIT_MSG}

          🔗 ${{ env.SITE_URL }}"

          curl -s -X POST "https://api.telegram.org/bot${{ env.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ env.TELEGRAM_CHAT_ID }}" \
            -d text="$TEXT" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview=true || true

      - name: Notify Telegram (failure)
        if: failure()
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          AUTHOR=$(git log -1 --pretty=%an)

          TEXT="❌ *SarvCast Backend Deploy Failed*

          📦 Commit: \`${COMMIT_HASH}\`
          👤 Author: ${AUTHOR}

          🔗 [View logs](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"

          curl -s -X POST "https://api.telegram.org/bot${{ env.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ env.TELEGRAM_CHAT_ID }}" \
            -d text="$TEXT" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview=true || true
